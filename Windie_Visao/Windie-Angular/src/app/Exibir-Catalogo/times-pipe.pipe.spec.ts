import { TimesPipePipe } from './times-pipe.pipe';

describe('TimesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
